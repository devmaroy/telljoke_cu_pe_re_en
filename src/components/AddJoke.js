import React from 'react';

const AddJoke = () => (
    <div>
        <form>
            <textarea rows="4"></textarea>
            <button>Add Joke</button>
        </form>
    </div>
);

export default AddJoke;